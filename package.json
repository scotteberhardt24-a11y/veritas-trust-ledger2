{
  "name": "veritas-trust-ledger",
  "private": true,
  "version": "1.0.0",

  "scripts": {
    "dev": "pnpm --filter veritas-web dev",
    "build": "pnpm --filter veritas-web build",
    "lint": "pnpm --filter veritas-web lint"
  }
}
