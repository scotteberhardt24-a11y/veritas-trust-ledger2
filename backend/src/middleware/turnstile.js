async function verifyTurnstile(r,s,n){if(!process.env.TURNSTILE_SECRET_KEY)return n();const t=r.body?.turnstileToken||r.headers['x-turnstile-token'];if(!t)return s.status(400).json({error:"Bot verification required"});try{const res=await fetch("https://challenges.cloudflare.com/turnstile/v0/siteverify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({secret:process.env.TURNSTILE_SECRET_KEY,response:t})});const d=await res.json();if(!d.success)return s.status(403).json({error:"Bot check failed"});n();}catch(e){n();}}
module.exports={verifyTurnstile};
